[project]
name = "juju-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chi Wai Chan", email = "chanchiwairay@gmail.com" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
juju-ai = "juju_ai:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
